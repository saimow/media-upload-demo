# 💻 Installation guide

`composer install`

`npm install`

create `.env` file and to generate the `APP_KEY` using:

`php artisan key:generate`

`php artisan migrate`

`php artisan db:seed`

`npm run dev`

`php artisan serve`